
{
  "name": "event-dynamics",
  "version": "0.1.0",
  "private": true,
  "description": "Event planning, logistics and ticketing platform.",
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=22.13.0 <23",
    "pnpm": ">=9.15.4 <10"
  },
  
  "scripts": {
    "build": "pnpm --filter api build",
    "dev": "pnpm --filter api start:dev",
    "lint": "pnpm --filter api lint",
    "lint:fix": "pnpm --filter api lint:fix",
    "test": "pnpm --filter api test",
    "test:e2e": "pnpm --filter api test:e2e"
  }
}
